// Test: Variable declarations (let, const, var)
// Expected: All variable types should work on Shelly

let name = "Shelly";
const VERSION = "1.0";
var legacy = true;

print("Device:", name);
print("Version:", VERSION);
print("Legacy mode:", legacy);
